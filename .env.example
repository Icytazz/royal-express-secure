# Template for .env - copy to .env and fill in real values.
# The real .env is gitignored and must never be committed.

DB_HOST=localhost
DB_NAME=royal_express_db
DB_USER=cms_app
DB_PASS=

# Directory for security.log and php-error.log. Must be outside the webroot.
LOG_DIR=
